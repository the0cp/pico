func add3(a, b, c) {
    return a + b + c;
}

func mix(x) {
    var a = add3(x, 1, 2);
    var b = add3(a, 3, 4);
    var c = add3(b, 5, 6);
    return c % 1000003;
}

var total = 0;

for (var i = 0; i < 1000000; i++) {
    total = total + mix(i);
}

if (total == 123456789) {
    print total;
}