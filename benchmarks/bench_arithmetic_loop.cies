var sum = 0;
var x = 1;

for (var i = 0; i < 3000000; i++) {
    x = x + 3;
    x = x * 2;
    x = x % 1000003;

    if (x % 2 == 0) {
        sum = sum + x;
    } else {
        sum = sum - x;
    }
}

if (sum == 123456789) {
    print sum;
}